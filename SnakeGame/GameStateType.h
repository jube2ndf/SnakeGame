#pragma once
namespace SnakeGame {
	enum class GameSceneType
	{
		None = 0,
		MainMenu,
		Settings,
		Complexity,
		Playing,
		GameOver,
		ExitDialog,
		Records,
		QuestionEnterName,
		EnterName,
		Pause
	};
}